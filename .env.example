DATABASE_URL="file:./dev.db"

# Google Sheets Sync
# The sync feature uses a Google Apps Script (GAS) Web App.
# The GAS URL is configured in src/lib/syncGoogleSheets.ts.
# No additional environment variables are required for the sync feature.
